export const metadata = {
  title: "Console",
  description:
    "Standalone AI console for quick prompt testing and direct model interaction.",
};

export default function ConsoleLayout({ children }) {
  return children;
}
