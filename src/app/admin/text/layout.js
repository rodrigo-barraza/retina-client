export const metadata = {
  title: "Text",
  description:
    "Manage text-to-speech and speech-to-text assets and history from the admin dashboard.",
};

export default function AdminTextLayout({ children }) {
  return children;
}
