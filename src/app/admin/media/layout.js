export const metadata = {
  title: "Media",
  description:
    "Browse and manage all AI-generated images, audio, and media assets across the Prism AI Gateway.",
};

export default function AdminMediaLayout({ children }) {
  return children;
}
