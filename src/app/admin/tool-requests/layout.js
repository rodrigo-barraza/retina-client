export const metadata = {
  title: "Tool Requests",
  description:
    "Browse, filter, and inspect tool-call telemetry from the tools-api with performance metrics and caller attribution.",
};

export default function ToolRequestsLayout({ children }) {
  return children;
}
