export const metadata = {
  title: "Workflows",
  description:
    "Review, manage, and inspect saved AI workflows and their execution history from the admin dashboard.",
};

export default function AdminWorkflowsLayout({ children }) {
  return children;
}
