export const metadata = {
  title: "Models",
  description:
    "Configure, enable, and manage AI models available through the Prism AI Gateway.",
};

export default function AdminModelsLayout({ children }) {
  return children;
}
