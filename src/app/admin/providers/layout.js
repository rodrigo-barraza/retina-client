export const metadata = {
  title: "Providers",
  description:
    "Manage and monitor connected AI providers, their endpoints, health status, and available models.",
};

export default function AdminProvidersLayout({ children }) {
  return children;
}
