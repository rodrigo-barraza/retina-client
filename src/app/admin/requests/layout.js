export const metadata = {
  title: "Requests",
  description:
    "Browse, filter, and inspect all API requests handled by the Prism AI Gateway with full payload and timing details.",
};

export default function AdminRequestsLayout({ children }) {
  return children;
}
