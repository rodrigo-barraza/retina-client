export const metadata = {
  title: "Conversations",
  description:
    "Monitor and inspect all AI conversations across projects. View messages, token usage, and generation status in real time.",
};

export default function AdminConversationsLayout({ children }) {
  return children;
}
