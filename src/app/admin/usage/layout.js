export const metadata = {
  title: "Usage",
  description:
    "Track API usage, token consumption, cost breakdowns, and per-project spending across the Prism AI Gateway.",
};

export default function AdminUsageLayout({ children }) {
  return children;
}
