export const metadata = {
  title: "Conversations",
  description:
    "Browse, search, and continue past AI conversations with full history and context.",
};

export default function ConversationsLayout({ children }) {
  return children;
}
